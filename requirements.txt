# Data collection
# >=1.9.0 is REQUIRED: it drives an undetected Chrome via Selenium to get past
# FBref's Cloudflare block. Earlier versions use tls-requests and get 403'd.
soccerdata>=1.9.0

# Analysis
pandas>=2.0
numpy>=1.24
scipy>=1.10            # statistical tests (KS / Mann-Whitney) for the comparison

# Visualisation
matplotlib>=3.7
